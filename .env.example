GITHUB_CLIENT_ID=your-github-oauth-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-client-secret
JWT_SECRET=change-this-to-random-string
JWT_REFRESH_SECRET=change-this-to-another-random-string
DATABASE_URL=postgres://k8squiz:k8squiz@localhost:5432/k8squiz?sslmode=disable
PROBLEMS_REPO_PATH=./problems
FRONTEND_URL=http://localhost:5173
DOCKER_HOST=unix:///var/run/docker.sock
SERVER_PORT=8080
# Global cap on active problem sessions; 0 = unlimited. Above the cap,
# POST /api/problems/:id/start returns 429.
MAX_CONCURRENT_SESSIONS=0
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_BASE_URL=https://api.openai.com/v1
# Container pre-warm pool (0 = disabled). Warm containers use the default
# Docker network, so enabling this relaxes per-session network isolation.
POOL_SIZE=0
POOL_IMAGE=k3s-base:latest
